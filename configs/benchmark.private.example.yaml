data_path: canonical-private.csv
artifact_root: ../private-artifacts
train_year: 2016
test_year: 2017
lags: [1, 2, 3]
weather_features: [avg_temp, humidity, rainfall]
categories: all
seeds: [42, 43, 44]
models: [seasonal_naive, arima, lstm, efs]
seasonal_period: 7
profile: full
efs:
  backend: matlab
  executable: matlab
  num_input_mfs: 2
  population_size: 100
  max_generations: 300
  crossover_fraction: 0.8
  pattern_max_iterations: 100
  use_parallel: true
